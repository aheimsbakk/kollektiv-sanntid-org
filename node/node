#!/usr/bin/env bash
# Minimal wrapper to run the specific Node.js binary installed under nvm
NODE_BIN="/home/opencode/.nvm/versions/node/v24.13.1/bin/node"

if [ ! -x "$NODE_BIN" ]; then
  echo "Error: Node binary not found or not executable: $NODE_BIN" >&2
  exit 1
fi

exec "$NODE_BIN" "$@"

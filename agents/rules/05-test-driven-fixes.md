# Rule: Test-Driven Bug Fixing
When asked to fix a bug or a regression, you MUST write a failing unit test that reproduces the exact bug BEFORE modifying the application code. Once the test fails, implement the fix to make the test pass. This prevents breaking existing functionality (regressions).
